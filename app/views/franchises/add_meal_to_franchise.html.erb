<header>
	<%= render 'layouts/navigation' %>
</header>
<%= form_tag(process_add_meal_to_franchise_path) do%>
<h1>Add meals to Menu for franchise <%=Franchise.find_by_id(params[:id]).id%></h1>
<table class="table table-striped table-bordered">
	<%=hidden_field_tag 'franchise', Franchise.find_by_id(params[:id]).id%>
	<thead>
		<tr>
			<th>Name</th>
			<th>Description</th>
			<th>Price</th>
			<th>Calories</th>
			<th>Select</th>
		</tr>
	</thead>
	<tbody>
		<% Meal.all.each do |meal| %>

		<tr>
			<td><%= meal.name %></td>
			<td><%= meal.description%></td>
			<td><%= meal.price %></td>
			<td><%= meal.calories %></td>
			<td><%= check_box_tag 'meals[meal_id][]', meal.id, false, :class => "hue" %></td>
		</tr>
		<% end %>
		  <%= hidden_field_tag 'franchise', Franchise.find_by_id(params[:id]).id%>

	</tbody>
</table>


<%= submit_tag 'Submit' %>

<%end%>
