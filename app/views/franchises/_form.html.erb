<%= form_for(@franchise) do |f| %>
  <% if @franchise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@franchise.errors.count, "error") %> prohibited this franchise from being saved:</h2>

      <ul>
      <% @franchise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="authform">

  <div class="form-group">
    <%= f.label :school_id %><br>
    <%= f.number_field :school_id, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Franchise', :class => 'button left' %>
    <%= link_to "Back", unauthenticated_root_path, :class => 'button right' %>
  </div>
 </div>
<% end %>
