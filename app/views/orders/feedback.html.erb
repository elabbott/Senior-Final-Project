<%= form_tag(process_feedback_path) do%>
<h1>Payment</h1>
<div class="field">
	<%= label_tag "Tell us what you think!" %>
	<br>
	<%= text_field_tag :feedback %>
	<%= hidden_field_tag :order_id, params[:id]  %>

</div>
<%= submit_tag 'Submit' %>

<%end%>
