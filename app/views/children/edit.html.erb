<header>
	<%= render 'layouts/navigation' %>
</header>

<h1>Approving Child:</h1>
<% if current_user.user_type != 4 %>
<%= render 'form' %>
<%= link_to 'Show', @child %> 
<% end %>
<% if current_user.user_type == 4 %>
<%= form_for(@child) do |c| %>
  <% if @child.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@child.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
      <% @child.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="form-group">
    <%= c.label :approved %><br>
    <%= c.check_box :approved %>
  </div>
  <div class="actions">
    <%= c.submit class: 'btn btn-success'%>
  </div>
  <% end %>
  <% end %>	

<%= link_to 'Back', authenticated_root_path %>
